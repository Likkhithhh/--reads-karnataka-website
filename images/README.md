# Image assets

The original READS website bundle contains the full image library used by the HTML pages, including brand, partner, board, event, community and hero images.

The connected GitHub integration used for this update can create text files but does not expose binary-file upload, so the JPG/PNG assets are not committed through this connector. The image paths in the HTML intentionally match the prepared project bundle.
